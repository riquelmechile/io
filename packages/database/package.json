{
  "name": "@io/database",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "PostgreSQL-shaped DbConnection port adapters for the trust-kernel persistence ports (first adapter slice). Not a canonical package.",
  "devDependencies": {
    "@io/business-domain": "workspace:*",
    "@io/trust-kernel": "workspace:*",
    "@types/pg": "^8.20.0"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
